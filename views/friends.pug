extends default_layout

block content
  h1= title

  if req && req.user

    each user in friends
      p
        a(href="/user/" + user.uid)
          div.profile-pic(style="background-image: url('" + user.profilePic + "')")
          = user.firstname + ' ' + user.lastname

  else
    p You will be escorted out
